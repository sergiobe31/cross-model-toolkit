{
  "name": "cross-model-toolkit",
  "description": "Cross-model collaboration: a second model via the PAL MCP (any OpenRouter model; GLM-5.2 by default) + /interceptor (idea→prompt) + /debate (adversarial chamber, tri-valued verdict) + pre-send secrets guard, with claim-by-claim adjudication.",
  "version": "1.4.0",
  "author": {
    "name": "Sergio"
  },
  "license": "MIT",
  "keywords": ["mcp", "glm", "cross-model", "interceptor", "debate", "openrouter"],
  "userConfig": {
    "openrouter_api_key": {
      "type": "string",
      "title": "OpenRouter API key",
      "description": "Your OpenRouter API key — powers the GLM-5.2 second model via the PAL MCP server. Get one at openrouter.ai. Stored in your system keychain, never in the repo.",
      "sensitive": true,
      "required": true
    }
  }
}
